def linear_search(array,target):
  comparision=0
  for i in range(len(array))
    comparision<=i
    if array[i]==target:
      return i, comparision
def binary_search(array,target):
  low=0
  comparision=0
  high=len(array)
  while low=high:
    comparision+=1
    mid=(low+high)//2
    elem=array(mid)
    if elem<target:
      low=mid
      if else ==target:
        low=mid
      if elem==target:
        return mid, comparision
      else:
        high=mid
      
    
    
def compare_search_algorithms(arr, target):
  
    
    return []